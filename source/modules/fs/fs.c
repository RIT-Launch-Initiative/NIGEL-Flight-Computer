#include "fs.h"

/**
 * Initialize the fs
 * @return status code
 */
int fs_init() {

    return 0;
}

/**
 * Writes data into a buffer
 * @return status code
 */
int fs_write() {
    return 0;
}

/**
 * Writes buffer into a file
 * @return status code
 */
int fs_dump() {
    return 0;
}

/**
 * Closes file
 * @return status code
 */
int fs_close() {
    return 0;
}